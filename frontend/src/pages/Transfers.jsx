import { Container, Typography } from "@mui/material";

export default function Transfers() {
  return (
    <Container sx={{ mt: 3 }}>
      <Typography variant="h5">Transfers</Typography>
      <Typography sx={{ mt: 2 }}>
        (Coming up) Transfer history + form to transfer assets.
      </Typography>
    </Container>
  );
}
